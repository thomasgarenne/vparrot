{% extends "base.html.twig" %}

{% block title %}Modifier Modèle{% endblock %}

{% block body %}
    <div class="w-75 m-auto min-vh-100">
        <h2>Modifier modèle {{ model.name }}</h2>
        {{ include('admin/models/_form.html.twig', {'btn': 'btn-warning', 'button_label': 'Modifier'}) }}
        <a href="{{ path('admin_models_index') }}">
            <button class="btn btn-info mt-2">Retour à la liste</button>
        </a>
    </div>
{% endblock %}