<div class="d-flex justify-content-around align-item-center">
    <p>DrivesCares</p>

    <table>
    <caption>Hours</caption>
        <thead>
            <td>Jour</td>
            <td>Open</td>
            <td>Closed</td>
            <td>Open</td>
            <td>Closed</td>
        </thead>
        {% set time = opening.getOpening %}
        
        {% for t in time %}
        <tr>
            <td>{{ t.day }}</td>
            <td>{{ t.openAm.format('H:i') }}</td>
            <td>{{ t.closeAm.format('H:i') }}</td>
            <td>{{ t.openPm.format('H:i') }}</td>
            <td>{{ t.closepm.format('H:i') }}</td>
        </tr>
        
        {% endfor %}

    </table>

    <div class="d-flex flex-column">
        <h3>Contact Us</h3>
        <span>10 rue du boulot</span>
        <span>33000 Bordeaux</span>
        <span>Nous rejoindre !</span>
        <span>00 11 22 33 44 55</span>
        <span>adresse@mail.fr</span>
    </div>

    <ul class="list-unstyled">
        <li>Accueil</li>
        <li>Nous contacter</li>
    </ul>
</div>

<div class="d-flex justify-content-center">
    <span class="text-align-center">TWIT</span>
    <span>FB</span>
    <span>INSTA</span>
</div>