{% extends "base.html.twig" %}

{% block title %}Ajout constructeur{% endblock %}

{% block body %}
    
    <div class="w-75 m-auto min-vh-100">
        <h2>Ajouter un constructeur</h2>
        {{ include('admin/brands/_form.html.twig') }}
        <a href="{{ path('admin_brands_index') }}">
            <button class="btn btn-info mt-2">Retour à la liste</button>
        </a>
    </div>

{% endblock %}