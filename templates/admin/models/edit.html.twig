{% extends "base.html.twig" %}

{% block title %}Edit Model{% endblock %}

{% block body %}
    <div class="col-9 m-auto">
        <h2>Modifier modèle {{ model.name }}</h2>

        <div class="w-75 m-auto">
            {% include "admin/models/_form.html.twig" %}

            <a href="{{ path('admin_models_index') }}">
                <button class="btn btn-info m-1">Retour à la liste</button>
            </a>
        </div>
    </div>
{% endblock %}