{% extends 'base.html.twig' %}

{% block title %}Nouveau type{% endblock %}

{% block body %}
    <div class="w-75 m-auto min-vh-100">
        <h2>Ajouter un Types</h2>
        {{ include('admin/types/_form.html.twig') }}
        <a href="{{ path('admin_types_index') }}">
            <button class="btn btn-info mt-2">Retour à la liste</button>
        </a>
    </div>

{% endblock %}
